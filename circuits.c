#include "circuits.h"

Bit36 bit36_and(Bit36 a, bool b)
{
    Bit36 out;
    out.bit0 = a.bit0 && b;
    out.bit1 = a.bit1 && b;
    out.bit2 = a.bit2 && b;
    out.bit3 = a.bit3 && b;
    out.bit4 = a.bit4 && b;
    out.bit5 = a.bit5 && b;
    out.bit6 = a.bit6 && b;
    out.bit7 = a.bit7 && b;
    out.bit8 = a.bit8 && b;
    out.bit9 = a.bit9 && b;
    out.bit10 = a.bit10 && b;
    out.bit11 = a.bit11 && b;
    out.bit12 = a.bit12 && b;
    out.bit13 = a.bit13 && b;
    out.bit14 = a.bit14 && b;
    out.bit15 = a.bit15 && b;
    out.bit16 = a.bit16 && b;
    out.bit17 = a.bit17 && b;
    out.bit18 = a.bit18 && b;
    out.bit19 = a.bit19 && b;
    out.bit20 = a.bit20 && b;
    out.bit21 = a.bit21 && b;
    out.bit22 = a.bit22 && b;
    out.bit23 = a.bit23 && b;
    out.bit24 = a.bit24 && b;
    out.bit25 = a.bit25 && b;
    out.bit26 = a.bit26 && b;
    out.bit27 = a.bit27 && b;
    out.bit28 = a.bit28 && b;
    out.bit29 = a.bit29 && b;
    out.bit30 = a.bit30 && b;
    out.bit31 = a.bit31 && b;
    out.bit32 = a.bit32 && b;
    out.bit33 = a.bit33 && b;
    out.bit34 = a.bit34 && b;
    out.bit35 = a.bit35 && b;

    return out;
}

Bit36 bit36_or(Bit36 a, Bit36 b)
{
    Bit36 out;
    out.bit0 = a.bit0 || b.bit0;
    out.bit1 = a.bit1 || b.bit1;
    out.bit2 = a.bit2 || b.bit2;
    out.bit3 = a.bit3 || b.bit3;
    out.bit4 = a.bit4 || b.bit4;
    out.bit5 = a.bit5 || b.bit5;
    out.bit6 = a.bit6 || b.bit6;
    out.bit7 = a.bit7 || b.bit7;
    out.bit8 = a.bit8 || b.bit8;
    out.bit9 = a.bit9 || b.bit9;
    out.bit10 = a.bit10 || b.bit10;
    out.bit11 = a.bit11 || b.bit11;
    out.bit12 = a.bit12 || b.bit12;
    out.bit13 = a.bit13 || b.bit13;
    out.bit14 = a.bit14 || b.bit14;
    out.bit15 = a.bit15 || b.bit15;
    out.bit16 = a.bit16 || b.bit16;
    out.bit17 = a.bit17 || b.bit17;
    out.bit18 = a.bit18 || b.bit18;
    out.bit19 = a.bit19 || b.bit19;
    out.bit20 = a.bit20 || b.bit20;
    out.bit21 = a.bit21 || b.bit21;
    out.bit22 = a.bit22 || b.bit22;
    out.bit23 = a.bit23 || b.bit23;
    out.bit24 = a.bit24 || b.bit24;
    out.bit25 = a.bit25 || b.bit25;
    out.bit26 = a.bit26 || b.bit26;
    out.bit27 = a.bit27 || b.bit27;
    out.bit28 = a.bit28 || b.bit28;
    out.bit29 = a.bit29 || b.bit29;
    out.bit30 = a.bit30 || b.bit30;
    out.bit31 = a.bit31 || b.bit31;
    out.bit32 = a.bit32 || b.bit32;
    out.bit33 = a.bit33 || b.bit33;
    out.bit34 = a.bit34 || b.bit34;
    out.bit35 = a.bit35 || b.bit35;

    return out;
}

bool bit2_multiplexer(Bit2 in, bool sel)
{
    bool out0 = in.bit0 && !sel;
    bool out1 = in.bit1 && sel;

    return out0 || out1;
}

bool bit4_multiplexer(Bit4 in, Bit2 sel)
{
    Bit2 in0;
    in0.bit0 = in.bit0;
    in0.bit1 = in.bit1;

    Bit2 in1;
    in1.bit0 = in.bit2;
    in1.bit1 = in.bit3;

    Bit2 out;
    out.bit0 = bit2_multiplexer(in0, sel.bit0);
    out.bit1 = bit2_multiplexer(in1, sel.bit0);

    return bit2_multiplexer(out, sel.bit1);
}

bool bit8_multiplexer(Bit8 in, Bit3 sel)
{
    Bit2 sel0;
    sel0.bit0 = sel.bit0;
    sel0.bit1 = sel.bit1;

    Bit4 in0;
    in0.bit0 = in.bit0;
    in0.bit1 = in.bit1;
    in0.bit2 = in.bit2;
    in0.bit3 = in.bit3;

    Bit4 in1;
    in1.bit0 = in.bit4;
    in1.bit1 = in.bit5;
    in1.bit2 = in.bit6;
    in1.bit3 = in.bit7;

    Bit2 out;
    out.bit0 = bit4_multiplexer(in0, sel0);
    out.bit1 = bit4_multiplexer(in1, sel0);

    return bit2_multiplexer(out, sel.bit2);
}

Bit2 bit1_decoder(bool sel)
{
    Bit2 out;
    out.bit0 = !sel;
    out.bit1 = sel;
    return out;
}

Bit4 bit2_decoder(Bit2 sel)
{
    Bit2 out0 = bit1_decoder(sel.bit0);
    Bit2 out1 = bit1_decoder(sel.bit1);

    Bit4 out;
    out.bit0 = out0.bit0 && out1.bit0;
    out.bit1 = out0.bit1 && out1.bit0;
    out.bit2 = out0.bit0 && out1.bit1;
    out.bit3 = out0.bit1 && out1.bit1;
    return out;
}

Bit8 bit3_decoder(Bit3 sel)
{
    Bit2 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;

    Bit4 out0 = bit2_decoder(in);

    Bit8 out;
    out.bit0 = out0.bit0 && !sel.bit2;
    out.bit1 = out0.bit1 && !sel.bit2;
    out.bit2 = out0.bit2 && !sel.bit2;
    out.bit3 = out0.bit3 && !sel.bit2;
    out.bit4 = out0.bit0 && sel.bit2;
    out.bit5 = out0.bit1 && sel.bit2;
    out.bit6 = out0.bit2 && sel.bit2;
    out.bit7 = out0.bit3 && sel.bit2;
    return out;
}

Bit16 bit4_decoder(Bit4 sel)
{
    Bit3 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;
    in.bit2 = sel.bit2;

    Bit8 out0 = bit3_decoder(in);

    Bit16 out;
    out.bit0 = out0.bit0 && !sel.bit3;
    out.bit1 = out0.bit1 && !sel.bit3;
    out.bit2 = out0.bit2 && !sel.bit3;
    out.bit3 = out0.bit3 && !sel.bit3;
    out.bit4 = out0.bit4 && !sel.bit3;
    out.bit5 = out0.bit5 && !sel.bit3;
    out.bit6 = out0.bit6 && !sel.bit3;
    out.bit7 = out0.bit7 && !sel.bit3;
    out.bit8 = out0.bit0 && sel.bit3;
    out.bit9 = out0.bit1 && sel.bit3;
    out.bit10 = out0.bit2 && sel.bit3;
    out.bit11 = out0.bit3 && sel.bit3;
    out.bit12 = out0.bit4 && sel.bit3;
    out.bit13 = out0.bit5 && sel.bit3;
    out.bit14 = out0.bit6 && sel.bit3;
    out.bit15 = out0.bit7 && sel.bit3;

    return out;
}

Bit32 bit5_decoder(Bit5 sel)
{
    Bit4 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;
    in.bit2 = sel.bit2;
    in.bit3 = sel.bit3;

    Bit16 out0 = bit4_decoder(in);

    Bit32 out;
    out.bit0 = out0.bit0 && !sel.bit4;
    out.bit1 = out0.bit1 && !sel.bit4;
    out.bit2 = out0.bit2 && !sel.bit4;
    out.bit3 = out0.bit3 && !sel.bit4;
    out.bit4 = out0.bit4 && !sel.bit4;
    out.bit5 = out0.bit5 && !sel.bit4;
    out.bit6 = out0.bit6 && !sel.bit4;
    out.bit7 = out0.bit7 && !sel.bit4;
    out.bit8 = out0.bit8 && !sel.bit4;
    out.bit9 = out0.bit9 && !sel.bit4;
    out.bit10 = out0.bit10 && !sel.bit4;
    out.bit11 = out0.bit11 && !sel.bit4;
    out.bit12 = out0.bit12 && !sel.bit4;
    out.bit13 = out0.bit13 && !sel.bit4;
    out.bit14 = out0.bit14 && !sel.bit4;
    out.bit15 = out0.bit15 && !sel.bit4;
    out.bit16 = out0.bit0 && sel.bit4;
    out.bit17 = out0.bit1 && sel.bit4;
    out.bit18 = out0.bit2 && sel.bit4;
    out.bit19 = out0.bit3 && sel.bit4;
    out.bit20 = out0.bit4 && sel.bit4;
    out.bit21 = out0.bit5 && sel.bit4;
    out.bit22 = out0.bit6 && sel.bit4;
    out.bit23 = out0.bit7 && sel.bit4;
    out.bit24 = out0.bit8 && sel.bit4;
    out.bit25 = out0.bit9 && sel.bit4;
    out.bit26 = out0.bit10 && sel.bit4;
    out.bit27 = out0.bit11 && sel.bit4;
    out.bit28 = out0.bit12 && sel.bit4;
    out.bit29 = out0.bit13 && sel.bit4;
    out.bit30 = out0.bit14 && sel.bit4;
    out.bit31 = out0.bit15 && sel.bit4;

    return out;
}

Bit64 bit6_decoder(Bit6 sel)
{
    Bit5 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;
    in.bit2 = sel.bit2;
    in.bit3 = sel.bit3;
    in.bit4 = sel.bit4;

    Bit32 out0 = bit5_decoder(in);

    Bit64 out;
    out.bit0 = out0.bit0 && !sel.bit5;
    out.bit1 = out0.bit1 && !sel.bit5;
    out.bit2 = out0.bit2 && !sel.bit5;
    out.bit3 = out0.bit3 && !sel.bit5;
    out.bit4 = out0.bit4 && !sel.bit5;
    out.bit5 = out0.bit5 && !sel.bit5;
    out.bit6 = out0.bit6 && !sel.bit5;
    out.bit7 = out0.bit7 && !sel.bit5;
    out.bit8 = out0.bit8 && !sel.bit5;
    out.bit9 = out0.bit9 && !sel.bit5;
    out.bit10 = out0.bit10 && !sel.bit5;
    out.bit11 = out0.bit11 && !sel.bit5;
    out.bit12 = out0.bit12 && !sel.bit5;
    out.bit13 = out0.bit13 && !sel.bit5;
    out.bit14 = out0.bit14 && !sel.bit5;
    out.bit15 = out0.bit15 && !sel.bit5;
    out.bit16 = out0.bit16 && !sel.bit5;
    out.bit17 = out0.bit17 && !sel.bit5;
    out.bit18 = out0.bit18 && !sel.bit5;
    out.bit19 = out0.bit19 && !sel.bit5;
    out.bit20 = out0.bit20 && !sel.bit5;
    out.bit21 = out0.bit21 && !sel.bit5;
    out.bit22 = out0.bit22 && !sel.bit5;
    out.bit23 = out0.bit23 && !sel.bit5;
    out.bit24 = out0.bit24 && !sel.bit5;
    out.bit25 = out0.bit25 && !sel.bit5;
    out.bit26 = out0.bit26 && !sel.bit5;
    out.bit27 = out0.bit27 && !sel.bit5;
    out.bit28 = out0.bit28 && !sel.bit5;
    out.bit29 = out0.bit29 && !sel.bit5;
    out.bit30 = out0.bit30 && !sel.bit5;
    out.bit31 = out0.bit31 && !sel.bit5;
    out.bit32 = out0.bit0 && sel.bit5;
    out.bit33 = out0.bit1 && sel.bit5;
    out.bit34 = out0.bit2 && sel.bit5;
    out.bit35 = out0.bit3 && sel.bit5;
    out.bit36 = out0.bit4 && sel.bit5;
    out.bit37 = out0.bit5 && sel.bit5;
    out.bit38 = out0.bit6 && sel.bit5;
    out.bit39 = out0.bit7 && sel.bit5;
    out.bit40 = out0.bit8 && sel.bit5;
    out.bit41 = out0.bit9 && sel.bit5;
    out.bit42 = out0.bit10 && sel.bit5;
    out.bit43 = out0.bit11 && sel.bit5;
    out.bit44 = out0.bit12 && sel.bit5;
    out.bit45 = out0.bit13 && sel.bit5;
    out.bit46 = out0.bit14 && sel.bit5;
    out.bit47 = out0.bit15 && sel.bit5;
    out.bit48 = out0.bit16 && sel.bit5;
    out.bit49 = out0.bit17 && sel.bit5;
    out.bit50 = out0.bit18 && sel.bit5;
    out.bit51 = out0.bit19 && sel.bit5;
    out.bit52 = out0.bit20 && sel.bit5;
    out.bit53 = out0.bit21 && sel.bit5;
    out.bit54 = out0.bit22 && sel.bit5;
    out.bit55 = out0.bit23 && sel.bit5;
    out.bit56 = out0.bit24 && sel.bit5;
    out.bit57 = out0.bit25 && sel.bit5;
    out.bit58 = out0.bit26 && sel.bit5;
    out.bit59 = out0.bit27 && sel.bit5;
    out.bit60 = out0.bit28 && sel.bit5;
    out.bit61 = out0.bit29 && sel.bit5;
    out.bit62 = out0.bit30 && sel.bit5;
    out.bit63 = out0.bit31 && sel.bit5;

    return out;
}

Bit128 bit7_decoder(Bit7 sel)
{
    Bit6 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;
    in.bit2 = sel.bit2;
    in.bit3 = sel.bit3;
    in.bit4 = sel.bit4;
    in.bit5 = sel.bit5;

    Bit64 out0 = bit6_decoder(in);

    Bit128 out;
    out.bit0 = out0.bit0 && !sel.bit6;
    out.bit1 = out0.bit1 && !sel.bit6;
    out.bit2 = out0.bit2 && !sel.bit6;
    out.bit3 = out0.bit3 && !sel.bit6;
    out.bit4 = out0.bit4 && !sel.bit6;
    out.bit5 = out0.bit5 && !sel.bit6;
    out.bit6 = out0.bit6 && !sel.bit6;
    out.bit7 = out0.bit7 && !sel.bit6;
    out.bit8 = out0.bit8 && !sel.bit6;
    out.bit9 = out0.bit9 && !sel.bit6;
    out.bit10 = out0.bit10 && !sel.bit6;
    out.bit11 = out0.bit11 && !sel.bit6;
    out.bit12 = out0.bit12 && !sel.bit6;
    out.bit13 = out0.bit13 && !sel.bit6;
    out.bit14 = out0.bit14 && !sel.bit6;
    out.bit15 = out0.bit15 && !sel.bit6;
    out.bit16 = out0.bit16 && !sel.bit6;
    out.bit17 = out0.bit17 && !sel.bit6;
    out.bit18 = out0.bit18 && !sel.bit6;
    out.bit19 = out0.bit19 && !sel.bit6;
    out.bit20 = out0.bit20 && !sel.bit6;
    out.bit21 = out0.bit21 && !sel.bit6;
    out.bit22 = out0.bit22 && !sel.bit6;
    out.bit23 = out0.bit23 && !sel.bit6;
    out.bit24 = out0.bit24 && !sel.bit6;
    out.bit25 = out0.bit25 && !sel.bit6;
    out.bit26 = out0.bit26 && !sel.bit6;
    out.bit27 = out0.bit27 && !sel.bit6;
    out.bit28 = out0.bit28 && !sel.bit6;
    out.bit29 = out0.bit29 && !sel.bit6;
    out.bit30 = out0.bit30 && !sel.bit6;
    out.bit31 = out0.bit31 && !sel.bit6;
    out.bit32 = out0.bit32 && !sel.bit6;
    out.bit33 = out0.bit33 && !sel.bit6;
    out.bit34 = out0.bit34 && !sel.bit6;
    out.bit35 = out0.bit35 && !sel.bit6;
    out.bit36 = out0.bit36 && !sel.bit6;
    out.bit37 = out0.bit37 && !sel.bit6;
    out.bit38 = out0.bit38 && !sel.bit6;
    out.bit39 = out0.bit39 && !sel.bit6;
    out.bit40 = out0.bit40 && !sel.bit6;
    out.bit41 = out0.bit41 && !sel.bit6;
    out.bit42 = out0.bit42 && !sel.bit6;
    out.bit43 = out0.bit43 && !sel.bit6;
    out.bit44 = out0.bit44 && !sel.bit6;
    out.bit45 = out0.bit45 && !sel.bit6;
    out.bit46 = out0.bit46 && !sel.bit6;
    out.bit47 = out0.bit47 && !sel.bit6;
    out.bit48 = out0.bit48 && !sel.bit6;
    out.bit49 = out0.bit49 && !sel.bit6;
    out.bit50 = out0.bit50 && !sel.bit6;
    out.bit51 = out0.bit51 && !sel.bit6;
    out.bit52 = out0.bit52 && !sel.bit6;
    out.bit53 = out0.bit53 && !sel.bit6;
    out.bit54 = out0.bit54 && !sel.bit6;
    out.bit55 = out0.bit55 && !sel.bit6;
    out.bit56 = out0.bit56 && !sel.bit6;
    out.bit57 = out0.bit57 && !sel.bit6;
    out.bit58 = out0.bit58 && !sel.bit6;
    out.bit59 = out0.bit59 && !sel.bit6;
    out.bit60 = out0.bit60 && !sel.bit6;
    out.bit61 = out0.bit61 && !sel.bit6;
    out.bit62 = out0.bit62 && !sel.bit6;
    out.bit63 = out0.bit63 && !sel.bit6;
    out.bit64 = out0.bit0 && sel.bit6;
    out.bit65 = out0.bit1 && sel.bit6;
    out.bit66 = out0.bit2 && sel.bit6;
    out.bit67 = out0.bit3 && sel.bit6;
    out.bit68 = out0.bit4 && sel.bit6;
    out.bit69 = out0.bit5 && sel.bit6;
    out.bit70 = out0.bit6 && sel.bit6;
    out.bit71 = out0.bit7 && sel.bit6;
    out.bit72 = out0.bit8 && sel.bit6;
    out.bit73 = out0.bit9 && sel.bit6;
    out.bit74 = out0.bit10 && sel.bit6;
    out.bit75 = out0.bit11 && sel.bit6;
    out.bit76 = out0.bit12 && sel.bit6;
    out.bit77 = out0.bit13 && sel.bit6;
    out.bit78 = out0.bit14 && sel.bit6;
    out.bit79 = out0.bit15 && sel.bit6;
    out.bit80 = out0.bit16 && sel.bit6;
    out.bit81 = out0.bit17 && sel.bit6;
    out.bit82 = out0.bit18 && sel.bit6;
    out.bit83 = out0.bit19 && sel.bit6;
    out.bit84 = out0.bit20 && sel.bit6;
    out.bit85 = out0.bit21 && sel.bit6;
    out.bit86 = out0.bit22 && sel.bit6;
    out.bit87 = out0.bit23 && sel.bit6;
    out.bit88 = out0.bit24 && sel.bit6;
    out.bit89 = out0.bit25 && sel.bit6;
    out.bit90 = out0.bit26 && sel.bit6;
    out.bit91 = out0.bit27 && sel.bit6;
    out.bit92 = out0.bit28 && sel.bit6;
    out.bit93 = out0.bit29 && sel.bit6;
    out.bit94 = out0.bit30 && sel.bit6;
    out.bit95 = out0.bit31 && sel.bit6;
    out.bit96 = out0.bit32 && sel.bit6;
    out.bit97 = out0.bit33 && sel.bit6;
    out.bit98 = out0.bit34 && sel.bit6;
    out.bit99 = out0.bit35 && sel.bit6;
    out.bit100 = out0.bit36 && sel.bit6;
    out.bit101 = out0.bit37 && sel.bit6;
    out.bit102 = out0.bit38 && sel.bit6;
    out.bit103 = out0.bit39 && sel.bit6;
    out.bit104 = out0.bit40 && sel.bit6;
    out.bit105 = out0.bit41 && sel.bit6;
    out.bit106 = out0.bit42 && sel.bit6;
    out.bit107 = out0.bit43 && sel.bit6;
    out.bit108 = out0.bit44 && sel.bit6;
    out.bit109 = out0.bit45 && sel.bit6;
    out.bit110 = out0.bit46 && sel.bit6;
    out.bit111 = out0.bit47 && sel.bit6;
    out.bit112 = out0.bit48 && sel.bit6;
    out.bit113 = out0.bit49 && sel.bit6;
    out.bit114 = out0.bit50 && sel.bit6;
    out.bit115 = out0.bit51 && sel.bit6;
    out.bit116 = out0.bit52 && sel.bit6;
    out.bit117 = out0.bit53 && sel.bit6;
    out.bit118 = out0.bit54 && sel.bit6;
    out.bit119 = out0.bit55 && sel.bit6;
    out.bit120 = out0.bit56 && sel.bit6;
    out.bit121 = out0.bit57 && sel.bit6;
    out.bit122 = out0.bit58 && sel.bit6;
    out.bit123 = out0.bit59 && sel.bit6;
    out.bit124 = out0.bit60 && sel.bit6;
    out.bit125 = out0.bit61 && sel.bit6;
    out.bit126 = out0.bit62 && sel.bit6;
    out.bit127 = out0.bit63 && sel.bit6;

    return out;
}

Bit256 bit8_decoder(Bit8 sel)
{
    Bit7 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;
    in.bit2 = sel.bit2;
    in.bit3 = sel.bit3;
    in.bit4 = sel.bit4;
    in.bit5 = sel.bit5;
    in.bit6 = sel.bit6;

    Bit128 out0 = bit7_decoder(in);

    Bit256 out;
    out.bit0 = out0.bit0 && !sel.bit7;
    out.bit1 = out0.bit1 && !sel.bit7;
    out.bit2 = out0.bit2 && !sel.bit7;
    out.bit3 = out0.bit3 && !sel.bit7;
    out.bit4 = out0.bit4 && !sel.bit7;
    out.bit5 = out0.bit5 && !sel.bit7;
    out.bit6 = out0.bit6 && !sel.bit7;
    out.bit7 = out0.bit7 && !sel.bit7;
    out.bit8 = out0.bit8 && !sel.bit7;
    out.bit9 = out0.bit9 && !sel.bit7;
    out.bit10 = out0.bit10 && !sel.bit7;
    out.bit11 = out0.bit11 && !sel.bit7;
    out.bit12 = out0.bit12 && !sel.bit7;
    out.bit13 = out0.bit13 && !sel.bit7;
    out.bit14 = out0.bit14 && !sel.bit7;
    out.bit15 = out0.bit15 && !sel.bit7;
    out.bit16 = out0.bit16 && !sel.bit7;
    out.bit17 = out0.bit17 && !sel.bit7;
    out.bit18 = out0.bit18 && !sel.bit7;
    out.bit19 = out0.bit19 && !sel.bit7;
    out.bit20 = out0.bit20 && !sel.bit7;
    out.bit21 = out0.bit21 && !sel.bit7;
    out.bit22 = out0.bit22 && !sel.bit7;
    out.bit23 = out0.bit23 && !sel.bit7;
    out.bit24 = out0.bit24 && !sel.bit7;
    out.bit25 = out0.bit25 && !sel.bit7;
    out.bit26 = out0.bit26 && !sel.bit7;
    out.bit27 = out0.bit27 && !sel.bit7;
    out.bit28 = out0.bit28 && !sel.bit7;
    out.bit29 = out0.bit29 && !sel.bit7;
    out.bit30 = out0.bit30 && !sel.bit7;
    out.bit31 = out0.bit31 && !sel.bit7;
    out.bit32 = out0.bit32 && !sel.bit7;
    out.bit33 = out0.bit33 && !sel.bit7;
    out.bit34 = out0.bit34 && !sel.bit7;
    out.bit35 = out0.bit35 && !sel.bit7;
    out.bit36 = out0.bit36 && !sel.bit7;
    out.bit37 = out0.bit37 && !sel.bit7;
    out.bit38 = out0.bit38 && !sel.bit7;
    out.bit39 = out0.bit39 && !sel.bit7;
    out.bit40 = out0.bit40 && !sel.bit7;
    out.bit41 = out0.bit41 && !sel.bit7;
    out.bit42 = out0.bit42 && !sel.bit7;
    out.bit43 = out0.bit43 && !sel.bit7;
    out.bit44 = out0.bit44 && !sel.bit7;
    out.bit45 = out0.bit45 && !sel.bit7;
    out.bit46 = out0.bit46 && !sel.bit7;
    out.bit47 = out0.bit47 && !sel.bit7;
    out.bit48 = out0.bit48 && !sel.bit7;
    out.bit49 = out0.bit49 && !sel.bit7;
    out.bit50 = out0.bit50 && !sel.bit7;
    out.bit51 = out0.bit51 && !sel.bit7;
    out.bit52 = out0.bit52 && !sel.bit7;
    out.bit53 = out0.bit53 && !sel.bit7;
    out.bit54 = out0.bit54 && !sel.bit7;
    out.bit55 = out0.bit55 && !sel.bit7;
    out.bit56 = out0.bit56 && !sel.bit7;
    out.bit57 = out0.bit57 && !sel.bit7;
    out.bit58 = out0.bit58 && !sel.bit7;
    out.bit59 = out0.bit59 && !sel.bit7;
    out.bit60 = out0.bit60 && !sel.bit7;
    out.bit61 = out0.bit61 && !sel.bit7;
    out.bit62 = out0.bit62 && !sel.bit7;
    out.bit63 = out0.bit63 && !sel.bit7;
    out.bit64 = out0.bit64 && !sel.bit7;
    out.bit65 = out0.bit65 && !sel.bit7;
    out.bit66 = out0.bit66 && !sel.bit7;
    out.bit67 = out0.bit67 && !sel.bit7;
    out.bit68 = out0.bit68 && !sel.bit7;
    out.bit69 = out0.bit69 && !sel.bit7;
    out.bit70 = out0.bit70 && !sel.bit7;
    out.bit71 = out0.bit71 && !sel.bit7;
    out.bit72 = out0.bit72 && !sel.bit7;
    out.bit73 = out0.bit73 && !sel.bit7;
    out.bit74 = out0.bit74 && !sel.bit7;
    out.bit75 = out0.bit75 && !sel.bit7;
    out.bit76 = out0.bit76 && !sel.bit7;
    out.bit77 = out0.bit77 && !sel.bit7;
    out.bit78 = out0.bit78 && !sel.bit7;
    out.bit79 = out0.bit79 && !sel.bit7;
    out.bit80 = out0.bit80 && !sel.bit7;
    out.bit81 = out0.bit81 && !sel.bit7;
    out.bit82 = out0.bit82 && !sel.bit7;
    out.bit83 = out0.bit83 && !sel.bit7;
    out.bit84 = out0.bit84 && !sel.bit7;
    out.bit85 = out0.bit85 && !sel.bit7;
    out.bit86 = out0.bit86 && !sel.bit7;
    out.bit87 = out0.bit87 && !sel.bit7;
    out.bit88 = out0.bit88 && !sel.bit7;
    out.bit89 = out0.bit89 && !sel.bit7;
    out.bit90 = out0.bit90 && !sel.bit7;
    out.bit91 = out0.bit91 && !sel.bit7;
    out.bit92 = out0.bit92 && !sel.bit7;
    out.bit93 = out0.bit93 && !sel.bit7;
    out.bit94 = out0.bit94 && !sel.bit7;
    out.bit95 = out0.bit95 && !sel.bit7;
    out.bit96 = out0.bit96 && !sel.bit7;
    out.bit97 = out0.bit97 && !sel.bit7;
    out.bit98 = out0.bit98 && !sel.bit7;
    out.bit99 = out0.bit99 && !sel.bit7;
    out.bit100 = out0.bit100 && !sel.bit7;
    out.bit101 = out0.bit101 && !sel.bit7;
    out.bit102 = out0.bit102 && !sel.bit7;
    out.bit103 = out0.bit103 && !sel.bit7;
    out.bit104 = out0.bit104 && !sel.bit7;
    out.bit105 = out0.bit105 && !sel.bit7;
    out.bit106 = out0.bit106 && !sel.bit7;
    out.bit107 = out0.bit107 && !sel.bit7;
    out.bit108 = out0.bit108 && !sel.bit7;
    out.bit109 = out0.bit109 && !sel.bit7;
    out.bit110 = out0.bit110 && !sel.bit7;
    out.bit111 = out0.bit111 && !sel.bit7;
    out.bit112 = out0.bit112 && !sel.bit7;
    out.bit113 = out0.bit113 && !sel.bit7;
    out.bit114 = out0.bit114 && !sel.bit7;
    out.bit115 = out0.bit115 && !sel.bit7;
    out.bit116 = out0.bit116 && !sel.bit7;
    out.bit117 = out0.bit117 && !sel.bit7;
    out.bit118 = out0.bit118 && !sel.bit7;
    out.bit119 = out0.bit119 && !sel.bit7;
    out.bit120 = out0.bit120 && !sel.bit7;
    out.bit121 = out0.bit121 && !sel.bit7;
    out.bit122 = out0.bit122 && !sel.bit7;
    out.bit123 = out0.bit123 && !sel.bit7;
    out.bit124 = out0.bit124 && !sel.bit7;
    out.bit125 = out0.bit125 && !sel.bit7;
    out.bit126 = out0.bit126 && !sel.bit7;
    out.bit127 = out0.bit127 && !sel.bit7;
    out.bit128 = out0.bit0 && sel.bit7;
    out.bit129 = out0.bit1 && sel.bit7;
    out.bit130 = out0.bit2 && sel.bit7;
    out.bit131 = out0.bit3 && sel.bit7;
    out.bit132 = out0.bit4 && sel.bit7;
    out.bit133 = out0.bit5 && sel.bit7;
    out.bit134 = out0.bit6 && sel.bit7;
    out.bit135 = out0.bit7 && sel.bit7;
    out.bit136 = out0.bit8 && sel.bit7;
    out.bit137 = out0.bit9 && sel.bit7;
    out.bit138 = out0.bit10 && sel.bit7;
    out.bit139 = out0.bit11 && sel.bit7;
    out.bit140 = out0.bit12 && sel.bit7;
    out.bit141 = out0.bit13 && sel.bit7;
    out.bit142 = out0.bit14 && sel.bit7;
    out.bit143 = out0.bit15 && sel.bit7;
    out.bit144 = out0.bit16 && sel.bit7;
    out.bit145 = out0.bit17 && sel.bit7;
    out.bit146 = out0.bit18 && sel.bit7;
    out.bit147 = out0.bit19 && sel.bit7;
    out.bit148 = out0.bit20 && sel.bit7;
    out.bit149 = out0.bit21 && sel.bit7;
    out.bit150 = out0.bit22 && sel.bit7;
    out.bit151 = out0.bit23 && sel.bit7;
    out.bit152 = out0.bit24 && sel.bit7;
    out.bit153 = out0.bit25 && sel.bit7;
    out.bit154 = out0.bit26 && sel.bit7;
    out.bit155 = out0.bit27 && sel.bit7;
    out.bit156 = out0.bit28 && sel.bit7;
    out.bit157 = out0.bit29 && sel.bit7;
    out.bit158 = out0.bit30 && sel.bit7;
    out.bit159 = out0.bit31 && sel.bit7;
    out.bit160 = out0.bit32 && sel.bit7;
    out.bit161 = out0.bit33 && sel.bit7;
    out.bit162 = out0.bit34 && sel.bit7;
    out.bit163 = out0.bit35 && sel.bit7;
    out.bit164 = out0.bit36 && sel.bit7;
    out.bit165 = out0.bit37 && sel.bit7;
    out.bit166 = out0.bit38 && sel.bit7;
    out.bit167 = out0.bit39 && sel.bit7;
    out.bit168 = out0.bit40 && sel.bit7;
    out.bit169 = out0.bit41 && sel.bit7;
    out.bit170 = out0.bit42 && sel.bit7;
    out.bit171 = out0.bit43 && sel.bit7;
    out.bit172 = out0.bit44 && sel.bit7;
    out.bit173 = out0.bit45 && sel.bit7;
    out.bit174 = out0.bit46 && sel.bit7;
    out.bit175 = out0.bit47 && sel.bit7;
    out.bit176 = out0.bit48 && sel.bit7;
    out.bit177 = out0.bit49 && sel.bit7;
    out.bit178 = out0.bit50 && sel.bit7;
    out.bit179 = out0.bit51 && sel.bit7;
    out.bit180 = out0.bit52 && sel.bit7;
    out.bit181 = out0.bit53 && sel.bit7;
    out.bit182 = out0.bit54 && sel.bit7;
    out.bit183 = out0.bit55 && sel.bit7;
    out.bit184 = out0.bit56 && sel.bit7;
    out.bit185 = out0.bit57 && sel.bit7;
    out.bit186 = out0.bit58 && sel.bit7;
    out.bit187 = out0.bit59 && sel.bit7;
    out.bit188 = out0.bit60 && sel.bit7;
    out.bit189 = out0.bit61 && sel.bit7;
    out.bit190 = out0.bit62 && sel.bit7;
    out.bit191 = out0.bit63 && sel.bit7;
    out.bit192 = out0.bit64 && sel.bit7;
    out.bit193 = out0.bit65 && sel.bit7;
    out.bit194 = out0.bit66 && sel.bit7;
    out.bit195 = out0.bit67 && sel.bit7;
    out.bit196 = out0.bit68 && sel.bit7;
    out.bit197 = out0.bit69 && sel.bit7;
    out.bit198 = out0.bit70 && sel.bit7;
    out.bit199 = out0.bit71 && sel.bit7;
    out.bit200 = out0.bit72 && sel.bit7;
    out.bit201 = out0.bit73 && sel.bit7;
    out.bit202 = out0.bit74 && sel.bit7;
    out.bit203 = out0.bit75 && sel.bit7;
    out.bit204 = out0.bit76 && sel.bit7;
    out.bit205 = out0.bit77 && sel.bit7;
    out.bit206 = out0.bit78 && sel.bit7;
    out.bit207 = out0.bit79 && sel.bit7;
    out.bit208 = out0.bit80 && sel.bit7;
    out.bit209 = out0.bit81 && sel.bit7;
    out.bit210 = out0.bit82 && sel.bit7;
    out.bit211 = out0.bit83 && sel.bit7;
    out.bit212 = out0.bit84 && sel.bit7;
    out.bit213 = out0.bit85 && sel.bit7;
    out.bit214 = out0.bit86 && sel.bit7;
    out.bit215 = out0.bit87 && sel.bit7;
    out.bit216 = out0.bit88 && sel.bit7;
    out.bit217 = out0.bit89 && sel.bit7;
    out.bit218 = out0.bit90 && sel.bit7;
    out.bit219 = out0.bit91 && sel.bit7;
    out.bit220 = out0.bit92 && sel.bit7;
    out.bit221 = out0.bit93 && sel.bit7;
    out.bit222 = out0.bit94 && sel.bit7;
    out.bit223 = out0.bit95 && sel.bit7;
    out.bit224 = out0.bit96 && sel.bit7;
    out.bit225 = out0.bit97 && sel.bit7;
    out.bit226 = out0.bit98 && sel.bit7;
    out.bit227 = out0.bit99 && sel.bit7;
    out.bit228 = out0.bit100 && sel.bit7;
    out.bit229 = out0.bit101 && sel.bit7;
    out.bit230 = out0.bit102 && sel.bit7;
    out.bit231 = out0.bit103 && sel.bit7;
    out.bit232 = out0.bit104 && sel.bit7;
    out.bit233 = out0.bit105 && sel.bit7;
    out.bit234 = out0.bit106 && sel.bit7;
    out.bit235 = out0.bit107 && sel.bit7;
    out.bit236 = out0.bit108 && sel.bit7;
    out.bit237 = out0.bit109 && sel.bit7;
    out.bit238 = out0.bit110 && sel.bit7;
    out.bit239 = out0.bit111 && sel.bit7;
    out.bit240 = out0.bit112 && sel.bit7;
    out.bit241 = out0.bit113 && sel.bit7;
    out.bit242 = out0.bit114 && sel.bit7;
    out.bit243 = out0.bit115 && sel.bit7;
    out.bit244 = out0.bit116 && sel.bit7;
    out.bit245 = out0.bit117 && sel.bit7;
    out.bit246 = out0.bit118 && sel.bit7;
    out.bit247 = out0.bit119 && sel.bit7;
    out.bit248 = out0.bit120 && sel.bit7;
    out.bit249 = out0.bit121 && sel.bit7;
    out.bit250 = out0.bit122 && sel.bit7;
    out.bit251 = out0.bit123 && sel.bit7;
    out.bit252 = out0.bit124 && sel.bit7;
    out.bit253 = out0.bit125 && sel.bit7;
    out.bit254 = out0.bit126 && sel.bit7;
    out.bit255 = out0.bit127 && sel.bit7;

    return out;
}

Bit512 bit9_decoder(Bit9 sel)
{
    Bit8 in;
    in.bit0 = sel.bit0;
    in.bit1 = sel.bit1;
    in.bit2 = sel.bit2;
    in.bit3 = sel.bit3;
    in.bit4 = sel.bit4;
    in.bit5 = sel.bit5;
    in.bit6 = sel.bit6;
    in.bit7 = sel.bit7;

    Bit256 out0 = bit8_decoder(in);

    Bit512 out;
    out.bit0 = out0.bit0 && !sel.bit8;
    out.bit1 = out0.bit1 && !sel.bit8;
    out.bit2 = out0.bit2 && !sel.bit8;
    out.bit3 = out0.bit3 && !sel.bit8;
    out.bit4 = out0.bit4 && !sel.bit8;
    out.bit5 = out0.bit5 && !sel.bit8;
    out.bit6 = out0.bit6 && !sel.bit8;
    out.bit7 = out0.bit7 && !sel.bit8;
    out.bit8 = out0.bit8 && !sel.bit8;
    out.bit9 = out0.bit9 && !sel.bit8;
    out.bit10 = out0.bit10 && !sel.bit8;
    out.bit11 = out0.bit11 && !sel.bit8;
    out.bit12 = out0.bit12 && !sel.bit8;
    out.bit13 = out0.bit13 && !sel.bit8;
    out.bit14 = out0.bit14 && !sel.bit8;
    out.bit15 = out0.bit15 && !sel.bit8;
    out.bit16 = out0.bit16 && !sel.bit8;
    out.bit17 = out0.bit17 && !sel.bit8;
    out.bit18 = out0.bit18 && !sel.bit8;
    out.bit19 = out0.bit19 && !sel.bit8;
    out.bit20 = out0.bit20 && !sel.bit8;
    out.bit21 = out0.bit21 && !sel.bit8;
    out.bit22 = out0.bit22 && !sel.bit8;
    out.bit23 = out0.bit23 && !sel.bit8;
    out.bit24 = out0.bit24 && !sel.bit8;
    out.bit25 = out0.bit25 && !sel.bit8;
    out.bit26 = out0.bit26 && !sel.bit8;
    out.bit27 = out0.bit27 && !sel.bit8;
    out.bit28 = out0.bit28 && !sel.bit8;
    out.bit29 = out0.bit29 && !sel.bit8;
    out.bit30 = out0.bit30 && !sel.bit8;
    out.bit31 = out0.bit31 && !sel.bit8;
    out.bit32 = out0.bit32 && !sel.bit8;
    out.bit33 = out0.bit33 && !sel.bit8;
    out.bit34 = out0.bit34 && !sel.bit8;
    out.bit35 = out0.bit35 && !sel.bit8;
    out.bit36 = out0.bit36 && !sel.bit8;
    out.bit37 = out0.bit37 && !sel.bit8;
    out.bit38 = out0.bit38 && !sel.bit8;
    out.bit39 = out0.bit39 && !sel.bit8;
    out.bit40 = out0.bit40 && !sel.bit8;
    out.bit41 = out0.bit41 && !sel.bit8;
    out.bit42 = out0.bit42 && !sel.bit8;
    out.bit43 = out0.bit43 && !sel.bit8;
    out.bit44 = out0.bit44 && !sel.bit8;
    out.bit45 = out0.bit45 && !sel.bit8;
    out.bit46 = out0.bit46 && !sel.bit8;
    out.bit47 = out0.bit47 && !sel.bit8;
    out.bit48 = out0.bit48 && !sel.bit8;
    out.bit49 = out0.bit49 && !sel.bit8;
    out.bit50 = out0.bit50 && !sel.bit8;
    out.bit51 = out0.bit51 && !sel.bit8;
    out.bit52 = out0.bit52 && !sel.bit8;
    out.bit53 = out0.bit53 && !sel.bit8;
    out.bit54 = out0.bit54 && !sel.bit8;
    out.bit55 = out0.bit55 && !sel.bit8;
    out.bit56 = out0.bit56 && !sel.bit8;
    out.bit57 = out0.bit57 && !sel.bit8;
    out.bit58 = out0.bit58 && !sel.bit8;
    out.bit59 = out0.bit59 && !sel.bit8;
    out.bit60 = out0.bit60 && !sel.bit8;
    out.bit61 = out0.bit61 && !sel.bit8;
    out.bit62 = out0.bit62 && !sel.bit8;
    out.bit63 = out0.bit63 && !sel.bit8;
    out.bit64 = out0.bit64 && !sel.bit8;
    out.bit65 = out0.bit65 && !sel.bit8;
    out.bit66 = out0.bit66 && !sel.bit8;
    out.bit67 = out0.bit67 && !sel.bit8;
    out.bit68 = out0.bit68 && !sel.bit8;
    out.bit69 = out0.bit69 && !sel.bit8;
    out.bit70 = out0.bit70 && !sel.bit8;
    out.bit71 = out0.bit71 && !sel.bit8;
    out.bit72 = out0.bit72 && !sel.bit8;
    out.bit73 = out0.bit73 && !sel.bit8;
    out.bit74 = out0.bit74 && !sel.bit8;
    out.bit75 = out0.bit75 && !sel.bit8;
    out.bit76 = out0.bit76 && !sel.bit8;
    out.bit77 = out0.bit77 && !sel.bit8;
    out.bit78 = out0.bit78 && !sel.bit8;
    out.bit79 = out0.bit79 && !sel.bit8;
    out.bit80 = out0.bit80 && !sel.bit8;
    out.bit81 = out0.bit81 && !sel.bit8;
    out.bit82 = out0.bit82 && !sel.bit8;
    out.bit83 = out0.bit83 && !sel.bit8;
    out.bit84 = out0.bit84 && !sel.bit8;
    out.bit85 = out0.bit85 && !sel.bit8;
    out.bit86 = out0.bit86 && !sel.bit8;
    out.bit87 = out0.bit87 && !sel.bit8;
    out.bit88 = out0.bit88 && !sel.bit8;
    out.bit89 = out0.bit89 && !sel.bit8;
    out.bit90 = out0.bit90 && !sel.bit8;
    out.bit91 = out0.bit91 && !sel.bit8;
    out.bit92 = out0.bit92 && !sel.bit8;
    out.bit93 = out0.bit93 && !sel.bit8;
    out.bit94 = out0.bit94 && !sel.bit8;
    out.bit95 = out0.bit95 && !sel.bit8;
    out.bit96 = out0.bit96 && !sel.bit8;
    out.bit97 = out0.bit97 && !sel.bit8;
    out.bit98 = out0.bit98 && !sel.bit8;
    out.bit99 = out0.bit99 && !sel.bit8;
    out.bit100 = out0.bit100 && !sel.bit8;
    out.bit101 = out0.bit101 && !sel.bit8;
    out.bit102 = out0.bit102 && !sel.bit8;
    out.bit103 = out0.bit103 && !sel.bit8;
    out.bit104 = out0.bit104 && !sel.bit8;
    out.bit105 = out0.bit105 && !sel.bit8;
    out.bit106 = out0.bit106 && !sel.bit8;
    out.bit107 = out0.bit107 && !sel.bit8;
    out.bit108 = out0.bit108 && !sel.bit8;
    out.bit109 = out0.bit109 && !sel.bit8;
    out.bit110 = out0.bit110 && !sel.bit8;
    out.bit111 = out0.bit111 && !sel.bit8;
    out.bit112 = out0.bit112 && !sel.bit8;
    out.bit113 = out0.bit113 && !sel.bit8;
    out.bit114 = out0.bit114 && !sel.bit8;
    out.bit115 = out0.bit115 && !sel.bit8;
    out.bit116 = out0.bit116 && !sel.bit8;
    out.bit117 = out0.bit117 && !sel.bit8;
    out.bit118 = out0.bit118 && !sel.bit8;
    out.bit119 = out0.bit119 && !sel.bit8;
    out.bit120 = out0.bit120 && !sel.bit8;
    out.bit121 = out0.bit121 && !sel.bit8;
    out.bit122 = out0.bit122 && !sel.bit8;
    out.bit123 = out0.bit123 && !sel.bit8;
    out.bit124 = out0.bit124 && !sel.bit8;
    out.bit125 = out0.bit125 && !sel.bit8;
    out.bit126 = out0.bit126 && !sel.bit8;
    out.bit127 = out0.bit127 && !sel.bit8;
    out.bit128 = out0.bit128 && !sel.bit8;
    out.bit129 = out0.bit129 && !sel.bit8;
    out.bit130 = out0.bit130 && !sel.bit8;
    out.bit131 = out0.bit131 && !sel.bit8;
    out.bit132 = out0.bit132 && !sel.bit8;
    out.bit133 = out0.bit133 && !sel.bit8;
    out.bit134 = out0.bit134 && !sel.bit8;
    out.bit135 = out0.bit135 && !sel.bit8;
    out.bit136 = out0.bit136 && !sel.bit8;
    out.bit137 = out0.bit137 && !sel.bit8;
    out.bit138 = out0.bit138 && !sel.bit8;
    out.bit139 = out0.bit139 && !sel.bit8;
    out.bit140 = out0.bit140 && !sel.bit8;
    out.bit141 = out0.bit141 && !sel.bit8;
    out.bit142 = out0.bit142 && !sel.bit8;
    out.bit143 = out0.bit143 && !sel.bit8;
    out.bit144 = out0.bit144 && !sel.bit8;
    out.bit145 = out0.bit145 && !sel.bit8;
    out.bit146 = out0.bit146 && !sel.bit8;
    out.bit147 = out0.bit147 && !sel.bit8;
    out.bit148 = out0.bit148 && !sel.bit8;
    out.bit149 = out0.bit149 && !sel.bit8;
    out.bit150 = out0.bit150 && !sel.bit8;
    out.bit151 = out0.bit151 && !sel.bit8;
    out.bit152 = out0.bit152 && !sel.bit8;
    out.bit153 = out0.bit153 && !sel.bit8;
    out.bit154 = out0.bit154 && !sel.bit8;
    out.bit155 = out0.bit155 && !sel.bit8;
    out.bit156 = out0.bit156 && !sel.bit8;
    out.bit157 = out0.bit157 && !sel.bit8;
    out.bit158 = out0.bit158 && !sel.bit8;
    out.bit159 = out0.bit159 && !sel.bit8;
    out.bit160 = out0.bit160 && !sel.bit8;
    out.bit161 = out0.bit161 && !sel.bit8;
    out.bit162 = out0.bit162 && !sel.bit8;
    out.bit163 = out0.bit163 && !sel.bit8;
    out.bit164 = out0.bit164 && !sel.bit8;
    out.bit165 = out0.bit165 && !sel.bit8;
    out.bit166 = out0.bit166 && !sel.bit8;
    out.bit167 = out0.bit167 && !sel.bit8;
    out.bit168 = out0.bit168 && !sel.bit8;
    out.bit169 = out0.bit169 && !sel.bit8;
    out.bit170 = out0.bit170 && !sel.bit8;
    out.bit171 = out0.bit171 && !sel.bit8;
    out.bit172 = out0.bit172 && !sel.bit8;
    out.bit173 = out0.bit173 && !sel.bit8;
    out.bit174 = out0.bit174 && !sel.bit8;
    out.bit175 = out0.bit175 && !sel.bit8;
    out.bit176 = out0.bit176 && !sel.bit8;
    out.bit177 = out0.bit177 && !sel.bit8;
    out.bit178 = out0.bit178 && !sel.bit8;
    out.bit179 = out0.bit179 && !sel.bit8;
    out.bit180 = out0.bit180 && !sel.bit8;
    out.bit181 = out0.bit181 && !sel.bit8;
    out.bit182 = out0.bit182 && !sel.bit8;
    out.bit183 = out0.bit183 && !sel.bit8;
    out.bit184 = out0.bit184 && !sel.bit8;
    out.bit185 = out0.bit185 && !sel.bit8;
    out.bit186 = out0.bit186 && !sel.bit8;
    out.bit187 = out0.bit187 && !sel.bit8;
    out.bit188 = out0.bit188 && !sel.bit8;
    out.bit189 = out0.bit189 && !sel.bit8;
    out.bit190 = out0.bit190 && !sel.bit8;
    out.bit191 = out0.bit191 && !sel.bit8;
    out.bit192 = out0.bit192 && !sel.bit8;
    out.bit193 = out0.bit193 && !sel.bit8;
    out.bit194 = out0.bit194 && !sel.bit8;
    out.bit195 = out0.bit195 && !sel.bit8;
    out.bit196 = out0.bit196 && !sel.bit8;
    out.bit197 = out0.bit197 && !sel.bit8;
    out.bit198 = out0.bit198 && !sel.bit8;
    out.bit199 = out0.bit199 && !sel.bit8;
    out.bit200 = out0.bit200 && !sel.bit8;
    out.bit201 = out0.bit201 && !sel.bit8;
    out.bit202 = out0.bit202 && !sel.bit8;
    out.bit203 = out0.bit203 && !sel.bit8;
    out.bit204 = out0.bit204 && !sel.bit8;
    out.bit205 = out0.bit205 && !sel.bit8;
    out.bit206 = out0.bit206 && !sel.bit8;
    out.bit207 = out0.bit207 && !sel.bit8;
    out.bit208 = out0.bit208 && !sel.bit8;
    out.bit209 = out0.bit209 && !sel.bit8;
    out.bit210 = out0.bit210 && !sel.bit8;
    out.bit211 = out0.bit211 && !sel.bit8;
    out.bit212 = out0.bit212 && !sel.bit8;
    out.bit213 = out0.bit213 && !sel.bit8;
    out.bit214 = out0.bit214 && !sel.bit8;
    out.bit215 = out0.bit215 && !sel.bit8;
    out.bit216 = out0.bit216 && !sel.bit8;
    out.bit217 = out0.bit217 && !sel.bit8;
    out.bit218 = out0.bit218 && !sel.bit8;
    out.bit219 = out0.bit219 && !sel.bit8;
    out.bit220 = out0.bit220 && !sel.bit8;
    out.bit221 = out0.bit221 && !sel.bit8;
    out.bit222 = out0.bit222 && !sel.bit8;
    out.bit223 = out0.bit223 && !sel.bit8;
    out.bit224 = out0.bit224 && !sel.bit8;
    out.bit225 = out0.bit225 && !sel.bit8;
    out.bit226 = out0.bit226 && !sel.bit8;
    out.bit227 = out0.bit227 && !sel.bit8;
    out.bit228 = out0.bit228 && !sel.bit8;
    out.bit229 = out0.bit229 && !sel.bit8;
    out.bit230 = out0.bit230 && !sel.bit8;
    out.bit231 = out0.bit231 && !sel.bit8;
    out.bit232 = out0.bit232 && !sel.bit8;
    out.bit233 = out0.bit233 && !sel.bit8;
    out.bit234 = out0.bit234 && !sel.bit8;
    out.bit235 = out0.bit235 && !sel.bit8;
    out.bit236 = out0.bit236 && !sel.bit8;
    out.bit237 = out0.bit237 && !sel.bit8;
    out.bit238 = out0.bit238 && !sel.bit8;
    out.bit239 = out0.bit239 && !sel.bit8;
    out.bit240 = out0.bit240 && !sel.bit8;
    out.bit241 = out0.bit241 && !sel.bit8;
    out.bit242 = out0.bit242 && !sel.bit8;
    out.bit243 = out0.bit243 && !sel.bit8;
    out.bit244 = out0.bit244 && !sel.bit8;
    out.bit245 = out0.bit245 && !sel.bit8;
    out.bit246 = out0.bit246 && !sel.bit8;
    out.bit247 = out0.bit247 && !sel.bit8;
    out.bit248 = out0.bit248 && !sel.bit8;
    out.bit249 = out0.bit249 && !sel.bit8;
    out.bit250 = out0.bit250 && !sel.bit8;
    out.bit251 = out0.bit251 && !sel.bit8;
    out.bit252 = out0.bit252 && !sel.bit8;
    out.bit253 = out0.bit253 && !sel.bit8;
    out.bit254 = out0.bit254 && !sel.bit8;
    out.bit255 = out0.bit255 && !sel.bit8;
    out.bit256 = out0.bit0 && sel.bit8;
    out.bit257 = out0.bit1 && sel.bit8;
    out.bit258 = out0.bit2 && sel.bit8;
    out.bit259 = out0.bit3 && sel.bit8;
    out.bit260 = out0.bit4 && sel.bit8;
    out.bit261 = out0.bit5 && sel.bit8;
    out.bit262 = out0.bit6 && sel.bit8;
    out.bit263 = out0.bit7 && sel.bit8;
    out.bit264 = out0.bit8 && sel.bit8;
    out.bit265 = out0.bit9 && sel.bit8;
    out.bit266 = out0.bit10 && sel.bit8;
    out.bit267 = out0.bit11 && sel.bit8;
    out.bit268 = out0.bit12 && sel.bit8;
    out.bit269 = out0.bit13 && sel.bit8;
    out.bit270 = out0.bit14 && sel.bit8;
    out.bit271 = out0.bit15 && sel.bit8;
    out.bit272 = out0.bit16 && sel.bit8;
    out.bit273 = out0.bit17 && sel.bit8;
    out.bit274 = out0.bit18 && sel.bit8;
    out.bit275 = out0.bit19 && sel.bit8;
    out.bit276 = out0.bit20 && sel.bit8;
    out.bit277 = out0.bit21 && sel.bit8;
    out.bit278 = out0.bit22 && sel.bit8;
    out.bit279 = out0.bit23 && sel.bit8;
    out.bit280 = out0.bit24 && sel.bit8;
    out.bit281 = out0.bit25 && sel.bit8;
    out.bit282 = out0.bit26 && sel.bit8;
    out.bit283 = out0.bit27 && sel.bit8;
    out.bit284 = out0.bit28 && sel.bit8;
    out.bit285 = out0.bit29 && sel.bit8;
    out.bit286 = out0.bit30 && sel.bit8;
    out.bit287 = out0.bit31 && sel.bit8;
    out.bit288 = out0.bit32 && sel.bit8;
    out.bit289 = out0.bit33 && sel.bit8;
    out.bit290 = out0.bit34 && sel.bit8;
    out.bit291 = out0.bit35 && sel.bit8;
    out.bit292 = out0.bit36 && sel.bit8;
    out.bit293 = out0.bit37 && sel.bit8;
    out.bit294 = out0.bit38 && sel.bit8;
    out.bit295 = out0.bit39 && sel.bit8;
    out.bit296 = out0.bit40 && sel.bit8;
    out.bit297 = out0.bit41 && sel.bit8;
    out.bit298 = out0.bit42 && sel.bit8;
    out.bit299 = out0.bit43 && sel.bit8;
    out.bit300 = out0.bit44 && sel.bit8;
    out.bit301 = out0.bit45 && sel.bit8;
    out.bit302 = out0.bit46 && sel.bit8;
    out.bit303 = out0.bit47 && sel.bit8;
    out.bit304 = out0.bit48 && sel.bit8;
    out.bit305 = out0.bit49 && sel.bit8;
    out.bit306 = out0.bit50 && sel.bit8;
    out.bit307 = out0.bit51 && sel.bit8;
    out.bit308 = out0.bit52 && sel.bit8;
    out.bit309 = out0.bit53 && sel.bit8;
    out.bit310 = out0.bit54 && sel.bit8;
    out.bit311 = out0.bit55 && sel.bit8;
    out.bit312 = out0.bit56 && sel.bit8;
    out.bit313 = out0.bit57 && sel.bit8;
    out.bit314 = out0.bit58 && sel.bit8;
    out.bit315 = out0.bit59 && sel.bit8;
    out.bit316 = out0.bit60 && sel.bit8;
    out.bit317 = out0.bit61 && sel.bit8;
    out.bit318 = out0.bit62 && sel.bit8;
    out.bit319 = out0.bit63 && sel.bit8;
    out.bit320 = out0.bit64 && sel.bit8;
    out.bit321 = out0.bit65 && sel.bit8;
    out.bit322 = out0.bit66 && sel.bit8;
    out.bit323 = out0.bit67 && sel.bit8;
    out.bit324 = out0.bit68 && sel.bit8;
    out.bit325 = out0.bit69 && sel.bit8;
    out.bit326 = out0.bit70 && sel.bit8;
    out.bit327 = out0.bit71 && sel.bit8;
    out.bit328 = out0.bit72 && sel.bit8;
    out.bit329 = out0.bit73 && sel.bit8;
    out.bit330 = out0.bit74 && sel.bit8;
    out.bit331 = out0.bit75 && sel.bit8;
    out.bit332 = out0.bit76 && sel.bit8;
    out.bit333 = out0.bit77 && sel.bit8;
    out.bit334 = out0.bit78 && sel.bit8;
    out.bit335 = out0.bit79 && sel.bit8;
    out.bit336 = out0.bit80 && sel.bit8;
    out.bit337 = out0.bit81 && sel.bit8;
    out.bit338 = out0.bit82 && sel.bit8;
    out.bit339 = out0.bit83 && sel.bit8;
    out.bit340 = out0.bit84 && sel.bit8;
    out.bit341 = out0.bit85 && sel.bit8;
    out.bit342 = out0.bit86 && sel.bit8;
    out.bit343 = out0.bit87 && sel.bit8;
    out.bit344 = out0.bit88 && sel.bit8;
    out.bit345 = out0.bit89 && sel.bit8;
    out.bit346 = out0.bit90 && sel.bit8;
    out.bit347 = out0.bit91 && sel.bit8;
    out.bit348 = out0.bit92 && sel.bit8;
    out.bit349 = out0.bit93 && sel.bit8;
    out.bit350 = out0.bit94 && sel.bit8;
    out.bit351 = out0.bit95 && sel.bit8;
    out.bit352 = out0.bit96 && sel.bit8;
    out.bit353 = out0.bit97 && sel.bit8;
    out.bit354 = out0.bit98 && sel.bit8;
    out.bit355 = out0.bit99 && sel.bit8;
    out.bit356 = out0.bit100 && sel.bit8;
    out.bit357 = out0.bit101 && sel.bit8;
    out.bit358 = out0.bit102 && sel.bit8;
    out.bit359 = out0.bit103 && sel.bit8;
    out.bit360 = out0.bit104 && sel.bit8;
    out.bit361 = out0.bit105 && sel.bit8;
    out.bit362 = out0.bit106 && sel.bit8;
    out.bit363 = out0.bit107 && sel.bit8;
    out.bit364 = out0.bit108 && sel.bit8;
    out.bit365 = out0.bit109 && sel.bit8;
    out.bit366 = out0.bit110 && sel.bit8;
    out.bit367 = out0.bit111 && sel.bit8;
    out.bit368 = out0.bit112 && sel.bit8;
    out.bit369 = out0.bit113 && sel.bit8;
    out.bit370 = out0.bit114 && sel.bit8;
    out.bit371 = out0.bit115 && sel.bit8;
    out.bit372 = out0.bit116 && sel.bit8;
    out.bit373 = out0.bit117 && sel.bit8;
    out.bit374 = out0.bit118 && sel.bit8;
    out.bit375 = out0.bit119 && sel.bit8;
    out.bit376 = out0.bit120 && sel.bit8;
    out.bit377 = out0.bit121 && sel.bit8;
    out.bit378 = out0.bit122 && sel.bit8;
    out.bit379 = out0.bit123 && sel.bit8;
    out.bit380 = out0.bit124 && sel.bit8;
    out.bit381 = out0.bit125 && sel.bit8;
    out.bit382 = out0.bit126 && sel.bit8;
    out.bit383 = out0.bit127 && sel.bit8;
    out.bit384 = out0.bit128 && sel.bit8;
    out.bit385 = out0.bit129 && sel.bit8;
    out.bit386 = out0.bit130 && sel.bit8;
    out.bit387 = out0.bit131 && sel.bit8;
    out.bit388 = out0.bit132 && sel.bit8;
    out.bit389 = out0.bit133 && sel.bit8;
    out.bit390 = out0.bit134 && sel.bit8;
    out.bit391 = out0.bit135 && sel.bit8;
    out.bit392 = out0.bit136 && sel.bit8;
    out.bit393 = out0.bit137 && sel.bit8;
    out.bit394 = out0.bit138 && sel.bit8;
    out.bit395 = out0.bit139 && sel.bit8;
    out.bit396 = out0.bit140 && sel.bit8;
    out.bit397 = out0.bit141 && sel.bit8;
    out.bit398 = out0.bit142 && sel.bit8;
    out.bit399 = out0.bit143 && sel.bit8;
    out.bit400 = out0.bit144 && sel.bit8;
    out.bit401 = out0.bit145 && sel.bit8;
    out.bit402 = out0.bit146 && sel.bit8;
    out.bit403 = out0.bit147 && sel.bit8;
    out.bit404 = out0.bit148 && sel.bit8;
    out.bit405 = out0.bit149 && sel.bit8;
    out.bit406 = out0.bit150 && sel.bit8;
    out.bit407 = out0.bit151 && sel.bit8;
    out.bit408 = out0.bit152 && sel.bit8;
    out.bit409 = out0.bit153 && sel.bit8;
    out.bit410 = out0.bit154 && sel.bit8;
    out.bit411 = out0.bit155 && sel.bit8;
    out.bit412 = out0.bit156 && sel.bit8;
    out.bit413 = out0.bit157 && sel.bit8;
    out.bit414 = out0.bit158 && sel.bit8;
    out.bit415 = out0.bit159 && sel.bit8;
    out.bit416 = out0.bit160 && sel.bit8;
    out.bit417 = out0.bit161 && sel.bit8;
    out.bit418 = out0.bit162 && sel.bit8;
    out.bit419 = out0.bit163 && sel.bit8;
    out.bit420 = out0.bit164 && sel.bit8;
    out.bit421 = out0.bit165 && sel.bit8;
    out.bit422 = out0.bit166 && sel.bit8;
    out.bit423 = out0.bit167 && sel.bit8;
    out.bit424 = out0.bit168 && sel.bit8;
    out.bit425 = out0.bit169 && sel.bit8;
    out.bit426 = out0.bit170 && sel.bit8;
    out.bit427 = out0.bit171 && sel.bit8;
    out.bit428 = out0.bit172 && sel.bit8;
    out.bit429 = out0.bit173 && sel.bit8;
    out.bit430 = out0.bit174 && sel.bit8;
    out.bit431 = out0.bit175 && sel.bit8;
    out.bit432 = out0.bit176 && sel.bit8;
    out.bit433 = out0.bit177 && sel.bit8;
    out.bit434 = out0.bit178 && sel.bit8;
    out.bit435 = out0.bit179 && sel.bit8;
    out.bit436 = out0.bit180 && sel.bit8;
    out.bit437 = out0.bit181 && sel.bit8;
    out.bit438 = out0.bit182 && sel.bit8;
    out.bit439 = out0.bit183 && sel.bit8;
    out.bit440 = out0.bit184 && sel.bit8;
    out.bit441 = out0.bit185 && sel.bit8;
    out.bit442 = out0.bit186 && sel.bit8;
    out.bit443 = out0.bit187 && sel.bit8;
    out.bit444 = out0.bit188 && sel.bit8;
    out.bit445 = out0.bit189 && sel.bit8;
    out.bit446 = out0.bit190 && sel.bit8;
    out.bit447 = out0.bit191 && sel.bit8;
    out.bit448 = out0.bit192 && sel.bit8;
    out.bit449 = out0.bit193 && sel.bit8;
    out.bit450 = out0.bit194 && sel.bit8;
    out.bit451 = out0.bit195 && sel.bit8;
    out.bit452 = out0.bit196 && sel.bit8;
    out.bit453 = out0.bit197 && sel.bit8;
    out.bit454 = out0.bit198 && sel.bit8;
    out.bit455 = out0.bit199 && sel.bit8;
    out.bit456 = out0.bit200 && sel.bit8;
    out.bit457 = out0.bit201 && sel.bit8;
    out.bit458 = out0.bit202 && sel.bit8;
    out.bit459 = out0.bit203 && sel.bit8;
    out.bit460 = out0.bit204 && sel.bit8;
    out.bit461 = out0.bit205 && sel.bit8;
    out.bit462 = out0.bit206 && sel.bit8;
    out.bit463 = out0.bit207 && sel.bit8;
    out.bit464 = out0.bit208 && sel.bit8;
    out.bit465 = out0.bit209 && sel.bit8;
    out.bit466 = out0.bit210 && sel.bit8;
    out.bit467 = out0.bit211 && sel.bit8;
    out.bit468 = out0.bit212 && sel.bit8;
    out.bit469 = out0.bit213 && sel.bit8;
    out.bit470 = out0.bit214 && sel.bit8;
    out.bit471 = out0.bit215 && sel.bit8;
    out.bit472 = out0.bit216 && sel.bit8;
    out.bit473 = out0.bit217 && sel.bit8;
    out.bit474 = out0.bit218 && sel.bit8;
    out.bit475 = out0.bit219 && sel.bit8;
    out.bit476 = out0.bit220 && sel.bit8;
    out.bit477 = out0.bit221 && sel.bit8;
    out.bit478 = out0.bit222 && sel.bit8;
    out.bit479 = out0.bit223 && sel.bit8;
    out.bit480 = out0.bit224 && sel.bit8;
    out.bit481 = out0.bit225 && sel.bit8;
    out.bit482 = out0.bit226 && sel.bit8;
    out.bit483 = out0.bit227 && sel.bit8;
    out.bit484 = out0.bit228 && sel.bit8;
    out.bit485 = out0.bit229 && sel.bit8;
    out.bit486 = out0.bit230 && sel.bit8;
    out.bit487 = out0.bit231 && sel.bit8;
    out.bit488 = out0.bit232 && sel.bit8;
    out.bit489 = out0.bit233 && sel.bit8;
    out.bit490 = out0.bit234 && sel.bit8;
    out.bit491 = out0.bit235 && sel.bit8;
    out.bit492 = out0.bit236 && sel.bit8;
    out.bit493 = out0.bit237 && sel.bit8;
    out.bit494 = out0.bit238 && sel.bit8;
    out.bit495 = out0.bit239 && sel.bit8;
    out.bit496 = out0.bit240 && sel.bit8;
    out.bit497 = out0.bit241 && sel.bit8;
    out.bit498 = out0.bit242 && sel.bit8;
    out.bit499 = out0.bit243 && sel.bit8;
    out.bit500 = out0.bit244 && sel.bit8;
    out.bit501 = out0.bit245 && sel.bit8;
    out.bit502 = out0.bit246 && sel.bit8;
    out.bit503 = out0.bit247 && sel.bit8;
    out.bit504 = out0.bit248 && sel.bit8;
    out.bit505 = out0.bit249 && sel.bit8;
    out.bit506 = out0.bit250 && sel.bit8;
    out.bit507 = out0.bit251 && sel.bit8;
    out.bit508 = out0.bit252 && sel.bit8;
    out.bit509 = out0.bit253 && sel.bit8;
    out.bit510 = out0.bit254 && sel.bit8;
    out.bit511 = out0.bit255 && sel.bit8;

    return out;
}

bool bit1_comparator(bool a, bool b)
{
    bool out = a ^ b;
    return !out;
}

bool bit2_comparator(Bit2 a, Bit2 b)
{
    bool out0 = bit1_comparator(a.bit0, b.bit0);
    bool out1 = bit1_comparator(a.bit1, b.bit1);

    return out0 && out1;
}

bool bit4_comparator(Bit4 a, Bit4 b)
{
    Bit2 a0;
    a0.bit0 = a.bit0;
    a0.bit1 = a.bit1;

    Bit2 a1;
    a1.bit0 = a.bit2;
    a1.bit1 = a.bit3;

    Bit2 b0;
    b0.bit0 = b.bit0;
    b0.bit1 = b.bit1;

    Bit2 b1;
    b1.bit0 = b.bit2;
    b1.bit1 = b.bit3;

    bool out0 = bit2_comparator(a0, b0);
    bool out1 = bit2_comparator(a1, b1);

    return out0 && out1;
}

Bit2 bit2_shifter_1(Bit2 in, bool sel)
{
    Bit2 out;
    out.bit0 = in.bit1 && sel;
    out.bit1 = in.bit0 && !sel;
    return out;
}

Bit4 bit4_shifter_1(Bit4 in, bool sel)
{
    Bit2 in0;
    in0.bit0 = in.bit0;
    in0.bit1 = in.bit1;

    Bit2 in1;
    in1.bit0 = in.bit2;
    in1.bit1 = in.bit3;

    Bit2 out0 = bit2_shifter_1(in0, sel);
    Bit2 out1 = bit2_shifter_1(in1, sel);

    Bit4 out;
    out.bit0 = out0.bit1 && sel;
    out.bit1 = out1.bit0 && sel || out0.bit0 && !sel;
    out.bit2 = out1.bit1 && sel || out0.bit1 && !sel;
    out.bit3 = out1.bit0 && !sel;
    return out;
}

Bit2 bit1_half_adder(bool a, bool b)
{
    bool sum = a ^ b;
    bool carry = a && b;

    Bit2 out;
    out.bit0 = sum;
    out.bit1 = carry;

    return out;
}

Bit2 bit1_full_adder(bool a, bool b, bool carry)
{
    Bit2 out0 = bit1_half_adder(a, b);
    Bit2 sum = bit1_half_adder(out0.bit0, carry);

    Bit2 out;
    out.bit0 = sum.bit0;
    out.bit1 = out0.bit1 || sum.bit1;
    return out;
}

Bit2 bit1_ALU(bool inva, bool a, bool ena, bool b, bool enb, bool carry, Bit2 sel)
{
    Bit4 c = bit2_decoder(sel);
    bool ina = (a && ena) ^ inva;
    bool inb = b && enb;

    bool out0 = ina && inb;
    bool out1 = ina || inb;
    bool out2 = !inb;
    Bit2 out3 = bit1_full_adder(ina, inb, carry);

    Bit2 out;
    out.bit0 = c.bit0 && out0 || c.bit1 && out1 || c.bit2 && out2 || c.bit3 && out3.bit0;
    out.bit1 = out3.bit1 && c.bit3;
    return out;
}
